import type { RuleConfig } from "../../types.ts";

export const miniGameRuleConfig: RuleConfig = {
  rules: [
    "You will be given three lives.",
    "If your attempt is wrong, you will lose one life.",
    "Get the correct answer to beat the question.",
  ],
};
